import { FilterPhonePipe } from './filter-phone.pipe';

describe('FilterPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
